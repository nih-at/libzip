language: c
compiler:
  - clang
  - gcc
os:
 - linux
 - osx
script:
 - mkdir build
 - cd build
 - cmake -DCMAKE_BUILD_TYPE=RELEASE ..
 - make
 - ctest . --config release
notifications:
  email:
    recipients:
      - build-status@libzip.org
    on_success: change
    on_failure: change
